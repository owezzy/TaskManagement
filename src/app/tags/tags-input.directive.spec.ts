import { TagsInputDirective } from './tags-input.directive';

describe('TagsInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TagsInputDirective();
    expect(directive).toBeTruthy();
  });
});
