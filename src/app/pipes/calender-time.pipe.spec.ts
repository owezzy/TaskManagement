import { CalenderTimePipe } from './calender-time.pipe';

describe('CalenderTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CalenderTimePipe();
    expect(pipe).toBeTruthy();
  });
});
