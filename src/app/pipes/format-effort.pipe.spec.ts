import { FormatEffortPipe } from './format-effort.pipe';

describe('FormatEffortPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEffortPipe();
    expect(pipe).toBeTruthy();
  });
});
